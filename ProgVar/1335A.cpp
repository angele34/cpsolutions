#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while (t--) {
        uint64_t n;
        cin >> n;
        uint64_t count = (n - 1) / 2;
        cout << count << endl;
    }
    return 0;
}